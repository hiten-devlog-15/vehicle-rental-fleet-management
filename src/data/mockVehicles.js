// mockVehicles.js — Experiment 2 mock data source
// Re-exports the vehicle array as the "mock data source" that useFetch() loads.
// In a real app this would be replaced by an API call (Experiment 4).

export { vehicles as mockVehicles, vehicleCategories } from './vehicles';
